# P0.4c leg 2

`$PYTHON` below is the requested absolute venv interpreter; its machine-local
path is omitted. Scope: trusted tests and mutation specs, ordinary phase isolation.

## 1. CLI

OBSERVED: real CLI subprocesses tested caught, survived, failed clean baseline,
and a two-item spec. Each phase also wrote a marker that the next phase required
to be absent. Source files stayed unchanged.

```text
4 passed, 146 deselected in 16.47s
caught mutation=1 file=victim.py: 1 test(s) ran; 1 failed; evidence=d226e818fb4b4ab8bc9ac4dc7d66d3da.json
SURVIVED mutation=1 file=victim.py: 1 test(s) ran; 0 failed; evidence=3c71bcd2509a4150bada7c824985885f.json
ERROR mutation=1 file=victim.py: clean baseline did not pass; evidence=42ff545a39a24e318e96cf378159bb35.json
```

Invocation shape (`$DEMO` is the test's independent committed fixture repository):

```sh
"$PYTHON" tests/mutation/mutate.py --backend container --repo "$DEMO" \
  --seed HEAD --file victim.py --old 1 --new 2 --test test_probe.py \
  --evidence-dir "$EVIDENCE" --scratch-dir "$SCRATCH"
```

Container results omit UNVERIFIED: caught means a clean baseline passed and the
mutant failed an ordinary test; SURVIVED means those tests still passed.
Exit codes are 0 for all caught, 1 for any survived, 2 for an execution error.
The macOS backend and its API remain available.
