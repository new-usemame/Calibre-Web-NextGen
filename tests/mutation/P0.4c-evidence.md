# P0.4c current behavior and verification

This replaces the historical leg notes and repeated-run transcript. The harness
now has a usable container CLI; the earlier unimplemented/blocked descriptions
no longer describe it. See ISOLATION.md for usage and limitations.

## Environment

OBSERVED on macOS, Python 3.12.7 from the project venv, Docker Linux engine
29.0.1. Normal pytest scratch remained on the configured external volume;
container scratch used its independent default. No CI files were changed.
Native Linux host execution was not checked: the Linux default demonstration
below overrides only the harness platform selection on macOS and runs real Docker.

## 1. Container test capability

The fixture requires the Docker CLI, a reachable Linux daemon, and a local
python:3.12 image. Missing capability skips the integration tests. It does not
pull images. CI needs no change to remain green without the image; to execute
these tests, provision python:3.12 first.

OBSERVED: a PATH shim forwarded real Docker except that `image inspect` exited 1.
Command: project venv Python -m pytest tests/unit/test_mutation_harness.py -q
-o addopts= -k container --maxfail=3 --tb=short

```text
8 passed, 19 skipped, 135 deselected in 10.33s
exit: 0
```

## 2. Platform default

macOS defaults to macos; other platforms default to container. Explicit
--backend selection remains available and has help text. Unit checks for Linux
and Windows selection passed (2 passed, 162 deselected in 1.00s).

OBSERVED: a temporary committed VALUE = 1 fixture with a test asserting VALUE == 1,
run with --seed COMMIT --file victim.py --old 1 --new 2 --test test_probe.py,
no --backend flag, under the Linux platform-selection proxy:

```text
CONTAINER seed=680d957205cd18e1ea148c6701598792b6617711
caught mutation=1 file=victim.py: 1 test(s) ran; 1 failed; evidence=1f19065a68704afb98b1d079345cfdd3.json
exit: 0
```

## 3. Missing report diagnostics

A phase that returns without a readable pytest report is retained in the JSON
phases list, including its return code and raw stdout/stderr. Successful phase
records retain the existing report and log hashes. Evidence files can therefore
contain test diagnostics; review them before sharing.

OBSERVED: a real container importing missing_test_dependency produced no report.
The regression first failed with an empty phases list:

```text
1 failed, 164 deselected in 1.74s
```

After the fix, it asserted retained startup stdout and stderr containing
`No module named 'missing_test_dependency'`:

```text
1 passed, 164 deselected in 1.69s
```

## 4. Full-suite checks and container inventory

Command (project venv Python):
`-m pytest tests/unit/test_mutation_harness.py -q -o addopts= -p no:rerunfailures -p no:flaky --tb=short -rf`

OBSERVED, no retries:

```text
Run 1: 165 passed in 194.11s (0:03:14)
Run 2: 2 failed, 163 passed in 190.70s (0:03:10)
Run 3: 165 passed in 202.54s (0:03:22)
```

Run 2 failures were in the existing Darwin tests:
- test_cli_spec_and_provenance_rejection_are_live[spec]: expected two payloads,
  got one after a console-probe provenance rejection.
- test_review_target_witness_accepts_real_local_execution[cps/__init__.py]:
  `cleanup=cannot inspect process environment: errno 5`.

These were not changed or retried. All container tests passed in all three attempts.
The suite is not consistently green; the second run is retained above.

OBSERVED `docker ps -a --format '{{.ID}} {{.Names}} {{.Status}}'` before:

```text
7528c4f7d132 cwn-local Exited (255) 23 hours ago
d4aa97f8aa38  Dead
```

After:

```text
7528c4f7d132 cwn-local Exited (255) 24 hours ago
d4aa97f8aa38  Dead
```

`docker ps -aq --filter label=org.cwng.mutation-phase` returned no output.
No pre-existing container or image was removed or modified.

Changes from the leg's starting commit are confined to ISOLATION.md, this document,
the deletion of the three stale P0.4c companion notes, container_backend.py,
mutate.py, and tests/unit/test_mutation_harness.py. No production or CI files changed.
