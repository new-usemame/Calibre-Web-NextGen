# SPD feature-request wave

- #498: implemented a per-user saved advanced-search default using `User.view_settings`; root renders the saved filter, reload preserves it, and Clear returns to Catalog. Unit tests and production frontend build pass. Live `cwn-spd` desktop and 360px mobile save → root/reload → automatic advanced view → clear → Catalog are OBSERVED. Luna found and the branch fixed a 6px mobile date-range overflow; final 360px rerun OBSERVED document and body `scrollWidth == clientWidth == 349`, no horizontal overflow, with unique accessible names for Published after/before, Search, and Reset. No new route.
- #878: implemented canonical API timestamps, Book Detail metadata rows, and sortable Date added / Last modified Table columns. Red/green serializer coverage (28 related tests green) and production frontend build pass; sortable headers follow the existing keyboard/`aria-sort` pattern. Live visual rerun pending after branch image rebuild.
- #783: designed as M safe core / L full spreadsheet. Implemented permission-gated single-title inline editing in the existing sortable table; relational/batch spreadsheet operations are explicitly deferred in `notes/783-bulk-metadata-DESIGN.md` with required semantics and concurrency work.
- #330: not viable for stock Nickel through Kobo sync; document the device-side current-cover setting. Design: `notes/330-kobo-sleepscreen-DESIGN.md`.
- #228: viable only as an optional MCP sidecar after operator SDK/runtime approval and a security review; no code/dependency added. Design: `notes/228-mcp-server-DESIGN.md`. Proposed future authenticated transport/API work must run `/security-review`.
- #548: blocked on operator direction and an upstream Wizarr provider contract; native invitations are recommended as the lower-trust-boundary alternative. No code/dependency added. Design: `notes/548-wizarr-DESIGN.md`. Either future path adds auth/user-management routes and must run `/security-review`.
