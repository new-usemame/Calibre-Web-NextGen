# P0.4c leg 3

Host observed: Darwin, Python 3.12.7 using the requested absolute venv interpreter.
Docker context: desktop-linux. The project selected its normal external-volume
pytest scratch; no TMPDIR, CWNG_PYTEST_TMP_BASE, or --basetemp override was used.
The pilot's synthetic Git fixture identity change was already committed and retained.

## 1. Scratch routing

One `docker_scratch` fixture supplies all live container bind sources and CLI
scratch arguments. Its default is `/tmp`, with explicit CWNG_DOCKER_SCRATCH
override, independent of pytest's TMPDIR and cached tempfile.tempdir. Repository
fixtures and ordinary pytest artifacts still use the project's own scratch.

OBSERVED regression against the previous tmp_path location choice:

```text
1 failed, 158 deselected in 0.22s
```

OBSERVED after routing correction, same normal pytest environment:

```text
1 passed, 158 deselected in 0.20s
```

At this point the Docker daemon was unavailable, so these two results establish
scratch routing only, not live Docker execution. Live results must be recorded
separately once Docker is available.

## 2. Fast failure

Create has a five-second timeout; the follow-up ownership lookup is also bounded
at five seconds. Other Docker operations retain their existing timeouts. Output:

```text
Docker could not create the phase container within 5 seconds. The scratch directory may not be shared. Use --scratch-dir /tmp or share the directory in Docker Desktop.
```

OBSERVED controlled regression before the change:

```text
2 failed, 159 deselected in 0.77s
AssertionError: unshareable scratch can hang for 30 seconds
```

OBSERVED after the change, including routing and CLI error-message checks:

```text
7 passed, 154 deselected in 3.13s
```

The blocked create and lost-reply cases above use a controlled subprocess/engine
model, not the unavailable Docker daemon. The lost-reply test requires removal
of the exact created container ID before returning the error.

## 3. Committed regression tests

The routing regression redirects both TMPDIR and Python's cached temporary
directory before requesting the fixture. The create regression rejects any
timeout above five seconds. Both were seen red before their fixes (above).
An additional real subprocess wait uses a sleeping Docker stand-in, so the
timeout and message are exercised without stopping or changing a real daemon.

OBSERVED, normal project pytest environment, no scratch override:

```text
Simulated stalled create: 5.01s; Docker could not create the phase container within 5 seconds. The scratch directory may not be shared. Use --scratch-dir /tmp or share the directory in Docker Desktop.
4 passed, 158 deselected in 5.69s
```

This is a real five-second process timeout with a simulated Docker create,
not a live test of Docker Desktop's external-volume sharing.

## Live verification status

NOT COMPLETED: three live container-suite runs, a real unshareable-mount check,
and before/after container inventories. Docker was unavailable in both local
contexts during this leg. The operator was asked whether Docker Desktop may be
started, because the daemon is shared; no answer had arrived at this checkpoint.
No real container was created during this leg and no Docker settings were changed.

OBSERVED before/after inventory attempts: `docker ps -a` exited 1; the daemon
could not be reached. This is not evidence of an empty container inventory.
OBSERVED actual CLI output, exit 2:

```text
ERROR: Cannot reach the Docker daemon. Start Docker and check your Docker context with docker info, then retry.
```

To finish in this environment once Docker is available, use the specified
interpreter for three normal invocations of `-m pytest
tests/unit/test_mutation_harness.py -q -o addopts= -p no:rerunfailures
-p no:flaky --tb=short`. Do not override the project's pytest scratch. These full
runs also cover leg 2's pending three-run requirement; `-k container` alone covers
only leg 3. A real external-volume mount failure still needs checking on the
running local Docker Desktop engine. No Linux-server host run is claimed.
