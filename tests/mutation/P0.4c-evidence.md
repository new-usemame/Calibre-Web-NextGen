# P0.4c evidence — authority blocked

All commands below use `$PYTHON`, the operator-specified absolute Python 3.12.7
venv interpreter. Its machine-local path is intentionally not recorded.
The checkout was clean at dc0a2e08ff on mod/p0.4c-linux-backend.

Common pytest arguments:

```sh
CWNG_PYTEST_TMP_BASE=/tmp/cwng-p04c "$PYTHON" -m pytest \
  tests/unit/test_mutation_harness.py -q -o addopts= --tb=short \
  --basetemp="$(mktemp -d /tmp/cwng-p04c-XXXXXX)"
```

OBSERVED: Docker server reports `linux 29.0.1`. The existing `python:3.12`
image resolves to
`sha256:cd7c412d000912f29075a1b8803e43cb2f38bb67f104019df526df5ceaf30569`
(Linux arm64). No image was built, pulled, removed, or pruned.

OBSERVED, initial `docker ps -a --format '{{.ID}} {{.Names}} {{.Status}}'`:

```text
7528c4f7d132 cwn-local Exited (255) 13 hours ago
d4aa97f8aa38  Dead
```

## 1. Container-per-phase primitive

OBSERVED: `container_backend.py:60` implements a pinned Git archive copied into
a fresh container for each phase. Only the disposable output directory is
mounted. The source checkout, shared Git directory and host interpreter are
not mounted. The image ID is resolved once. Container removal precedes return.
The primitive is not yet wired into the mutation CLI or granted verdict authority.

OBSERVED negative controls, common command plus selections/env below:

```text
CWNG_CONTAINER_TEST_FAULT=cleanup
-k 'test_container_tokenless or test_container_setup_failure'
4 failed, 132 deselected in 9.31s
exit: assert remaining == '' (container still present)
timeout: AssertionError: tokenless detached writer survived container removal
failure: assert remaining == '' (container still present)
setup interruption: container still present

CWNG_CONTAINER_TEST_FAULT=seed
-k test_container_phases_copy
1 failed, 135 deselected in 1.04s
AssertionError: pinned source VALUE = 1 check failed inside container
```

The fault mode lives exclusively in the test fixture, which removes its exact
created container IDs after the assertions, including during failed tests.
The cleanup fault also suppresses the runner's own removal check so the tests
independently witness the defect. No unrelated containers are removed.

OBSERVED green, common command plus `-k test_container_ -rP --show-capture=stdout`:

```text
CONTAINMENT exit: alive_writes=4 after_removal_writes=0 containers=0
CONTAINMENT timeout: alive_writes=20 after_removal_writes=0 containers=0
CONTAINMENT failure: alive_writes=4 after_removal_writes=0 containers=0
5 passed, 131 deselected in 10.44s
```

These are counts in the shared output file, sampled again one second after
removal; the writer runs every 0.1 seconds and is independently finite (8 seconds)
to bound intentionally defective test runs. Tests also assert the container ID
is absent. Kernel lifetime semantics, rather than that finite sampling interval,
are the basis for descendant containment.

OBSERVED environment limitation: the default external-volume pytest scratch
caused Docker bind-mount creation timeouts. Reusing a deleted/recreated explicit
base directory also produced bind-source-not-found errors despite host existence.
Unique scratch under `/tmp` passed. No Docker settings were modified. Initial
missing-module errors and mount failures are NOT counted as useful negative controls.

ASSUMED: Docker daemon and kernel remain available to honor removal. Host process
SIGKILL, daemon failure, and a create request completing after a lost reply are
not established cleanup guarantees by these tests. This primitive must not be
represented as a completed authoritative backend.

## 2. Authoritative verdict gate — NOT IMPLEMENTED

OBSERVED: the existing diagnostic result types and `present_checked_result`
remain unchanged. The new primitive also has frozen UNVERIFIED/false labels;
it is not accepted by the existing concrete-type presentation gate. It cannot
emit authoritative mutation verdicts. No CLI backend-selection argument was added.

OBSERVED blocker: running the actual `pytest_evidence.py` observer and the
unchanged leg7 fixture bodies in Linux containers leaves all four defeat vectors
undetected. The kernel boundary establishes process lifetime, not which Python
code object supplied an observed frame. Consequently removal success is not
sufficient evidence to grant verdict authority under the requested contract.

The implementation stops at this boundary per the task's stop condition. This
does NOT claim that every possible external execution-attestation design is
impossible. No external byte-execution verifier was built or tested here.
Authenticating an in-process report or comparing source-file read hashes would
not fix the demonstrated code-substitution cases.

## 3. Four vectors — reproduced, NOT CONTAINED/DETECTED as requested

OBSERVED requirement check, common command with
`CWNG_REQUIRE_CONTAINER_PROVENANCE=1 -k test_container_leg7 -rP --show-capture=stdout`:

```text
LINUX PROBE clean_control: exit=1 seen=True active=True signal=TEST_FAILURE authority=False
LINUX PROBE absent_control: exit=0 seen=False active=True signal=TESTS_PASSED authority=False
LINUX PROBE startup_rewrite: exit=0 seen=True active=True signal=TESTS_PASSED authority=False
LINUX PROBE frame_forge: exit=0 seen=True active=True signal=TESTS_PASSED authority=False
LINUX PROBE meta_transform: exit=0 seen=True active=True signal=TESTS_PASSED authority=False
LINUX PROBE loader_transform: exit=0 seen=True active=True signal=TESTS_PASSED authority=False
4 failed, 2 passed, 136 deselected in 8.40s
```

All four failures are the explicit requirement assertion:
`container removal did not detect substituted target execution`.
The two loader vectors additionally wrote the SHA-256 of the intended mutant
bytes, and the host test checked equality. A matching read hash did not prevent
execution of different code. The absent control correctly had `seen=False`;
`TESTS_PASSED` there is the execution validator's signal only, not acceptance by
the higher-level target-provenance gate.

The normal regression asserts this observed limitation. Its green result must
not be read as achieving detection: the explicit requirement check above remains
red by design. No xfail or skip hides that failing requirement.

OBSERVED normal regression plus unchanged old forge tests, common command with
`-k 'test_container_leg7 or review_forged_authority' -rP --show-capture=stdout`:

```text
REVIEW3 shape=setattr PRESENT=REJECTED (Mac/APFS only)
REVIEW3 shape=duck PRESENT=REJECTED (Mac/APFS only)
8 passed, 134 deselected in 8.27s
```

The six probe output lines match the requirement run above. The normal run
passes because it tests the limitation, not because substituted execution is
detected. Host test interpreter is the requested venv; container interpreter
comes from the pinned Linux image. Pytest and its pure-Python dependencies are
copied from that venv into the disposable tree; no network install is used.

## 4. Limits in code and output

OBSERVED: `container_backend.py` states the external-delegation exclusions in
its module docstring and `LIMITS` literal; `present_observation` prints that
literal and always returns 1. It rejects forged authority fields, subclasses and
duck types. It does not render arbitrary phase stdout as a trusted message.
`ISOLATION.md` also records the blocked status and cleanup assumptions.

OBSERVED, common command plus
`-k 'test_container_output or test_container_presentation'`:

```text
Before the presenter existed:
4 failed, 142 deselected in 0.26s
AttributeError: module 'container_backend' has no attribute 'present_observation'

After implementation:
4 passed, 142 deselected in 0.13s
```

The output test checks every named exclusion; the other three cases require
rejection of forged fields, a subclass, and a duck type. This verifies a
diagnostic-only presenter, NOT deliverable 2's authoritative gate.

OBSERVED output from the same selection with `-rP --show-capture=stdout`:

```text
UNVERIFIED Linux container phase observation. Outside containment: externally delegated work in databases, network services, shared ports, remote service managers, and daemons outside the container. Not hermetic. Execution provenance is UNVERIFIED.
4 passed, 142 deselected in 0.19s
```

## Repeat-run protocol and CI feasibility

The full suite here means all tests in `tests/unit/test_mutation_harness.py`,
including the original macOS-specific cases and all fifteen new cases. The
five-run record is `P0.4c-repeated-runs.txt`. Each invocation uses the common
command above without `-k`, plus `-p no:rerunfailures -p no:flaky -rP
--show-capture=stdout`. Fault-injection, platform-proxy, and requirement-check
environment overrides are cleared. Each invocation gets unique `/tmp` scratch.
Only selected literal evidence lines and real pytest summaries are persisted;
raw tracebacks and machine-local paths are not recorded.

OBSERVED: `.github/workflows/tests.yml:54` gives `fast-tests` 25 minutes overall;
`:116` gives its smoke/unit pytest step 10 minutes. That command selects `unit`
and uses `--dist=loadfile`, so this test file stays together on one worker.
The new container tests are marked unit through the existing module-level mark.

OBSERVED incremental measurements: the lifecycle selection took 10.44 seconds,
the six provenance cases plus two old forge cases took 8.27 seconds, and the
four presenter cases took 0.19 seconds. This is approximately 19 seconds of new
test work with the image already available. Tests create containers sequentially,
each capped at one CPU, 256 MiB, and 64 PIDs.

ASSUMED CI feasibility: that added runtime is compatible with `fast-tests`'s
10-minute test budget; a separate job is not required by these measurements.
Total existing-job headroom and a cold image pull were not measured. Before
enabling these tests in CI, provision a trusted Linux Python 3.12 image locally
under `python:3.12` (prefer a pinned multi-architecture digest), and make Docker
available to the test user. The runner resolves the image ID once and uses
`--pull=never`. A missing image/daemon is a failure when the Docker CLI exists;
absence of the CLI is an explicit fixture skip. No CI or workflow file was edited.

The host suite ran with the specified Python 3.12.7 venv. Linux code ran in Docker
on this machine, not on a remote host. This does not establish a native Linux run
of the entire host-side suite or the timing of the existing hosted CI job.

OBSERVED run 2 failure identification: immediately after that invocation,
`.pytest_cache/v/cache/lastfailed` named these three existing tests. Reading only
their own retained durable evidence JSON produced the following details:

```text
test_checked_real_outcomes_are_durable_and_unverified[setup_error]
ERROR cannot inspect process environment: errno 5

test_cli_spec_and_provenance_rejection_are_live[spec]
ERROR provenance REJECTED: console probe execution failed (exit=0, timeout=False, cleanup=cannot inspect process environment: errno 5)

test_review_target_witness_accepts_real_local_execution[cps/__init__.py]
ERROR provenance REJECTED: child probe execution failed (exit=0, timeout=False, cleanup=cannot inspect process environment: errno 5)
```

These are macOS diagnostic process-inspection errors, not container test failures.
`P0.4b-leg9-evidence.md` already records errno-5/EIO failures in previous full
runs. No change to that backend was made here, and these failures are not hidden
or treated as passes. Five full attempts do not establish five green runs.

## Scope reconciliation

OBSERVED: the requested starting commit is `dc0a2e08ff`. Current `origin/main`
at the scope audit is `679ac40cf272d4f143ed98600c5653b652e04e97`, whose one
additional commit is `fix(kobo): warn on oversized sync response headers (#2163)`.
The branch was kept at the requested seed; it was not rebased onto that commit.

Consequently `git diff --name-only origin/main..HEAD` includes upstream-only
differences as well as this slice. Reconciliation, one row per path (including
the final repeat-run evidence artifact):

| Path | Explanation |
| --- | --- |
| `changelog.d/1857-kobo-sync-header-budget.md` | Upstream-only addition absent at the assigned seed; never edited here. |
| `cps/kobo.py` | Upstream-only changes after the assigned seed; never edited here. |
| `tests/mutation/ISOLATION.md` | This slice: blocked Linux primitive and scope limits. |
| `tests/mutation/P0.4c-evidence.md` | This slice: commands, actual outputs, blockers, and scope reconciliation. |
| `tests/mutation/P0.4c-repeated-runs.txt` | This slice: five full harness attempts, including failures. |
| `tests/mutation/container_backend.py` | This slice: container-per-phase diagnostic primitive and presenter. |
| `tests/mutation/container_probe.py` | This slice: real pytest runtime overlay for unchanged leg7 fixtures. |
| `tests/unit/test_kobo_sync_header_budget.py` | Upstream-only addition absent at the assigned seed; never edited here. |
| `tests/unit/test_kobo_synctoken_compression_331.py` | Upstream-only changes after the assigned seed; never edited here. |
| `tests/unit/test_mutation_harness.py` | This slice: fifteen additional cases and explicit negative-control fixture. Existing cases unchanged. |

`git diff --name-status dc0a2e08ff..HEAD` isolates this branch's authored changes:
only the six permitted mutation paths above. No production, CI, workflow, or
other test file was modified by this slice. No push, PR, or merge was performed.

## Final five-run results and Docker cleanup

OBSERVED, complete file each time, no retries or maxfail:

```text
RUN 1: 146 passed in 173.36s (0:02:53)
RUN 2: 3 failed, 143 passed in 167.61s (0:02:47)
RUN 3: 2 failed, 144 passed in 168.67s (0:02:48)
RUN 4: 146 passed in 174.01s (0:02:54)
RUN 5: 2 failed, 144 passed in 174.58s (0:02:54)
```

All fifteen new tests passed in every invocation. The original macOS forge
tests passed unchanged in every invocation. The full suite is NOT consistently
green. Run 2's failures are identified above. Run 3 failed the existing ignored
poison case with an explicit errno-5 process-inspection error and the existing
broken-baseline case with an empty trace where collection/baseline were expected.
Run 5 failed the existing xfail-run and timeout cases because child-preflight
cleanup reported errno 5 before the intended checks. The complete selected
failure details are in `P0.4c-repeated-runs.txt`. Runs 3–5 used `-rfP` instead of
`-rP` to include failed node IDs; this changes reporting, not test execution.

OBSERVED final `docker ps -a --format '{{.ID}} {{.Names}} {{.Status}}'`:

```text
7528c4f7d132 cwn-local Exited (255) 14 hours ago
d4aa97f8aa38  Dead
```

OBSERVED final `docker ps -aq --filter label=org.cwng.mutation-phase`:
no output. The same two pre-existing containers remain; none created by this
slice remains. No image was created or removed. The temporary negative-control
leaks were removed by their test fixture before the next test.

OBSERVED: `git diff --check dc0a2e08ff..HEAD` passed. The test-file diff reports
196 added lines and zero removed lines. Source code was unchanged across all five
full runs; only evidence documentation changed afterward.

Final status: deliverable 1 has a proved process-boundary primitive, but CLI
integration and abnormal-host-death recovery are unfinished. Deliverable 2 is
not implemented. Deliverable 3's four detection requirements remain red.
Deliverable 4 is implemented for the diagnostic primitive. The requested
authoritative backend and five-green-run completion condition are NOT achieved.
